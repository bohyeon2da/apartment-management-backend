spring:
  application:
    name: apartment-management

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/apartment_management}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:password}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

# JWT 설정
jwt:
  secret: ${JWT_SECRET:your-production-secret-key-change-this-in-railway}
  expiration: 86400000

# SMS 설정 (프로덕션에서는 실제 SMS 서비스 사용)
sms:
  enabled: ${SMS_ENABLED:false}

# CORS 설정 (Netlify 도메인 추가 예정)
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,https://*.netlify.app}

# 로깅
logging:
  level:
    root: INFO
    com.apartment.management: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
